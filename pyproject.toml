[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "kagravity"
version = "0.1.2"
dependencies = [
    "numba",
    "pint",
]
license = {text = "MIT"}

[tool.setuptools]
package-dir = {"" = "src"}
