[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "kagravity"
version="0.1.5"
dependencies = [
    "numba",
    "numpy<2.0.0",
    "pint",
]
license = {text = "MIT"}

[tool.setuptools]
package-dir = {"" = "src"}
